import React from "react";
import logo from "../logo.png";

const Header = () => {
    return (
        <header>
            <img src={logo} alt="Netflix logo" />
        </header>
    );
};

export default Header;
